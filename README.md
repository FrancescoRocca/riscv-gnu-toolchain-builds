# RISC-V GNU Toolchain Builds

Prebuilt `riscv-gnu-toolchain` binaries with multilib enabled.

## Quick start

1. Download the latest release and unzip it.
2. Set `RISCV` and update `PATH`.

Example:

```bash
export RISCV=/path/to/riscv
export PATH="$PATH:$RISCV/bin"
```
